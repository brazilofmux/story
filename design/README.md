# Design

Architectural sketches and design notes for the story-telling engine. Each sketch is numbered and dated; revisions do not overwrite predecessors. Old sketches are left in place as a record of how the design changed, so the reasoning over time is preserved.

## Convention

- Files named `{topic}-sketch-NN.md` where NN is sequential per topic. A *topic* is a coherent design area — `substrate`, `architecture`, `descriptions`, and so on. Each topic has its own sequence.
- Every sketch has a **Status** (*draft, active*, *superseded*, *abandoned*) and a **Date** at the top.
- **Active sketches are self-contained within their topic.** A reader should be able to understand the current state of *that topic* from the active sketch alone, without chasing a chain of deltas. When corrections pile up, consolidate into a new sketch rather than let the active starting point depend on superseded context.
- **Cross-topic sketches are expected.** Some sketches (notably architecture-level ones) apply across topics. They do not supersede the sketches they constrain; they add a frame that future topic sketches are expected to satisfy. Reading a topic sketch alone gives the reader that topic's current state; reading the cross-topic sketches additionally gives them the frame those topic sketches sit inside.
- When a sketch is superseded, its status is updated and a link to the successor is added — but the file stays.
- Sketches are not specifications. They are the design thinking in progress. Claims and commitments may be wrong. Open questions are first-class.

## Active sketches

These are the sketches to read. Each is self-contained within its topic; no need to chase predecessors for that topic. Cross-topic sketches (architecture) sit alongside and add a frame.

**Cross-topic (applies across topics):**

- [Architecture — sketch 01](architecture-sketch-01.md) — grid-snap scope, two-surface semantics (facts vs descriptions), test for schema inclusion, descriptions draft attention. Frames all substrate and descriptions work. See its *Relation to substrate-sketch-04* section for how it interacts with the current substrate sketch, including the one point of genuine tension (F1).
- [Architecture — sketch 02](architecture-sketch-02.md) — the story-engine as a stack of dialects with the substrate as sink. Commitments A6–A11 (multi-dialect stack; author-driven lowering between dialects; automated verification at each boundary emitting observations to the existing proposal queue; each verifier speaks the upper dialect's vocabulary against the lower dialect's queries; dialects are opt-in and plural; the reader-model probe generalizes as cross-boundary partner). Extends A1–A5 to each dialect independently. Commits to the *shape* of the stack, not to any specific upper dialect — Dramatica, Save-the-Cat, Freytag, Aristotelian, and author-defined templates are all admitted. Frames every future prescriptive / upper-layer sketch.

**Per topic:**

- [Substrate — sketch 05](substrate-sketch-05.md) — consolidated substrate, aligned with architecture-sketch-01. Retires F1 (emotion/tension as parallel projection); adds M1 (adverbial/modal routing rule); makes event type an extensible categorical tag rather than a dispatch key; applies A3 retroactively to every carried-forward commitment. Current substrate statement; read alone for the substrate's shape.
- [Descriptions — sketch 01](descriptions-sketch-01.md) — the fold-invisible interpretive peer surface to the substrate's typed facts. Commitments D1–D5 (fold-invisibility, attention affordances, anchor-attachment, branch-default-from-anchor, no-auto-promotion). Specifies the description record, kind vocabulary, attention levels, review state with staleness under anchor edits, promotion mechanics via proposal queue, and tooling obligations. Absorbs substrate provenance.
- [Reader-model — sketch 01](reader-model-sketch-01.md) — how human or LLM interpretation of descriptions is invoked, reviewed, and fed back into authorial acts. Commitments R1–R6 (typed I/O, descriptions labeled in the view, partner-not-fallback, shared review-entry type across humans and LLMs, declared-scope invocations, anchor-τ_a staleness). Specifies the ReaderView, invocation modes (interactive, triggered, batch), the typed output shape (reviews, promotion proposals, question-answers), refusal/malformed handling, and the API/firewall boundary between substrate and tooling. The first prototype probe (substrate.py: `reader_view`, `ingest_review`, `ingest_proposal`; 11 tests in `test_rashomon.py`) exercises the view + review plumbing against the Rashomon encoding.
- [Identity and realization — sketch 01](identity-and-realization-sketch-01.md) — closes one bounded slice of substrate-sketch-05 open question 2 (the inference model): identity as a first-class proposition plus query-time substitution. Commitments I1–I7 (identity as typed Prop, per-fold scoping, query-time substitution with no state mutation, symmetry and transitivity, realization as identity-assertion trigger without held-set rewrite, no identity synthesis, KNOWN-only substitution). Specifies the substitution rule, multi-match resolution (strongest slot wins), agent and world query surfaces (`holds_literal` / `holds` / `holds_all_matches` / `equivalence_classes` / `world_holds`), branch interaction, and the Jocasta anagnorisis worked example. Retires the "realization rewrites held set" workaround in favor of "realization asserts identity; substitution does the rewrite at query time." General forward-chaining / domain-rule inference remains OQ2 for a future sketch.
- [Focalization — sketch 01](focalization-sketch-01.md) — closes the known weakening in `project_reader` (focalizer recorded as metadata only, no semantic effect). Commitments F1–F6 (focalization constrains disclosure slot via `min(author, focalizer)` under KNOWN>BELIEVED>SUSPECTED>GAP; focalization-driven demotion never overrides stronger prior reader state, distinguished from explicit author demotion; reference τ_s is the narrated event's τ_s; omniscient default unchanged; focalizer access is substitution-aware per I3/I7; narrator intrusion and external focalization deferred). Side-steps the "τ_d-scoped reader-state tracking" concern from substrate-04 by writing-rather-than-rewriting: focalization constrains disclosures as they land, never re-visits prior entries' effects. Turns the prototype's "deliberate deferred weakening" into proper semantics without new machinery.
- [Inference model — sketch 01](inference-model-sketch-01.md) — closes the remainder of substrate-sketch-05 OQ2 after identity-and-realization carved out the realization slice. Commitments N1–N10 (Horn clauses with conjunctive bodies; rules authored per-story; derivation is query-time; composes with identity substitution; per-fold scoping; weakest-premise slot propagation under KNOWN>BELIEVED>SUSPECTED>GAP, GAP premises fail; bounded fixpoint with depth cap 3; derived facts carry proofs; derivable boundary is definitional-over-world-props per the probe criterion; authored facts win). Retires the Oedipus encoding's author-asserted `parricide` and `incest` compound predicates in favor of two rules; vindicates the reader's reader-outruns-character position by showing the reader derives both conclusions from disclosed premises at τ_d=0. Out of scope: negation, disjunction, existentials, rule synthesis, rule τ_a.
- [Dramatic dialect — sketch 01](dramatic-sketch-01.md) — first upper-dialect sketch under architecture-sketch-02's stack. Written on the dialect's own terms without reaching for substrate vocabulary (events, props, folds, identities, rules are not mentioned); drafted under the discipline that dialects earn their shape by being coherent standalone before connective machinery is added. Commitments M1–M10 (zero-or-more Arguments per Story; Throughlines as roles not characters with `Throughline.owners` the single authoritative ownership source; Scenes as units of argumentative work with declared purpose; narrative_position as the dialect's time; Character Functions as template-parameterized with per-function multiplicity — exactly-one / at-most-one / at-least-one / any; Stakes first-class; grid-snap = declarations structural, landings interpretive; self-verification purely within Dramatic vocabulary, including template multiplicity checks; soft template conformance; description surface mirrors the cross-dialect pattern in Dramatic-native kinds). Dramatica-influenced but not Dramatica-bound: admits zero-or-many Arguments, variable Throughline counts, multiple Character Function templates (dramatica-8 with eight singular slots, three-actor, two-actor, ensemble, author-defined). Worked example: Oedipus Rex in Dramatic terms. Connective relations to other dialects are out of scope (architecture-sketch-02 concern).
- [Dramatica template — sketch 01](dramatica-template-sketch-01.md) — depth probe of the Dramatic dialect by encoding Dramatica's full Grand Argument Story theory as a specific Template (`dramatica-complete`) within the dialect. Same no-substrate-vocabulary discipline as dramatic-sketch-01; terminology clarified (Dramatic = the generalized dialect, Dramatica = the narrative theory / product, dramatica-complete = the Template). Commitments Q1–Q10 (Templates may extend the dialect with their own record types and Story-level fields; Quad as the core Dramatica structural unit; Domain/Concern/Issue/Problem as a nested quad hierarchy; Story Goal and Story Consequence as Template-specific Story-level fields; six Dynamic Story Points per Story; eight Character Functions with required Throughline assignments per Dramatica theory; four Signposts per Throughline; Dramatica's justification / process / approach interpretive apparatus lives in descriptions not records; Template-specific self-verification composes with M8; dialect-level gaps surface as forcing functions rather than silent failures). Worked examples: six full encodings against `dramatica-complete` (Oedipus Rex, Macbeth, *The Murder of Roger Ackroyd*, *Pride and Prejudice*, *Rocky*, *Chinatown*), closing the canonical Outcome × Judgment matrix across all four corners — personal-tragedy (×3), triumph (P&P), personal-triumph (Rocky), tragedy (Chinatown) — and exercising every DSP axis direction at least once across the corpus. Structurally distinct routes: different domain arrangements, Change vs. Steadfast MCs, Do-er vs. Be-er approaches, Optionlock vs. Timelock. The sketch ships the full character-element decomposition (Motivation + Methodology + Evaluation + Purpose = 64 elements across 4 dimensions, 16 Issue Quads as canonical theory data). The sketch's load-bearing output remains its *Where the dialect resists* section, surfacing six concrete forcing functions for a future dramatic-sketch-02: Template-level schema extension (M5 is too thin to carry Dramatica as-is); derived-field semantics for Solution/Symptom/Response; Story-level Template extensions; per-Function Throughline-assignment constraints; heavy Template theory-data (~300+ labels; 256 Element Quads still deferred); quad-shape typed validators. Sandwich question (whether anything sits between Dramatica and substrate) explicitly left open.
- [Lowering — sketch 01](lowering-sketch-01.md) — design exercise (exploratory, findings-heavy rather than commitment-heavy) attempting a concrete lowering from the dramatica-complete Oedipus encoding down to the `oedipus.py` substrate encoding, to pressure architecture-sketch-02's lowering claim with a real case. Central finding: upper-to-lower coupling comes in four distinct kinds — **Realization** (upper record made true by specific lower records; uses Lowering per A7), **Characterization** (upper record classifies a pattern the substrate exhibits; uses verification per A8), **Claim** (upper record asserts something about substrate state at a moment or across a trajectory; uses verification per A8), and **Flavor** (free-form author metadata; no formal check). Lowering records are the right mechanism for Realization *only*; the temptation to fold every upper-to-lower relationship into Lowering is wrong. Findings F1–F7: the four coupling kinds (F1); Realization targets are heterogeneous — typed records, record sets, descriptions — while query-shaped couplings belong to the verifier surface not to Lowerings (F2); some upper records couple via the description surface (F3); position correspondence between upper and lower dialects is author-declared (F4); substrate gaps surface concretely — Tiresias, Creon, post-anagnorisis plot beats are missing from `oedipus.py` (F5); Lowering (A7) and Verification (A8) are genuinely different relationships, neither reducible to the other (F6); derivation composes with verification — the inference-01 rule surface is what makes many Claim-coupling checks tractable (F7). Answers the sandwich question: direct Dramatica→substrate coupling works for most upper records; a middle dialect (Structural) would make some Realization-couplings more natural but is not strictly required. Hands a concrete brief to a future `lowering-record-sketch-01` (seven requirements, now refined by the four-kinds distinction) and names follow-on actions including extending `oedipus.py` to cover the cut plot beats and running a cross-boundary reader-model probe.
- [Lowering — sketch 02](lowering-sketch-02.md) — second lowering exercise parallel to sketch 01, using *Macbeth* as a structurally different test case (action-first MC, moral rather than epistemic trajectory, multiple Antagonist readings, supernatural Throughline participants, cumulative rather than momentary Judgment=Bad). Sketches both upper (dramatica-complete) and lower (substrate, as prose — no `macbeth.py` yet) encodings just deeply enough for the coupling walk. Central result: the four-kinds framework from sketch 01 held up; deliberately probed three candidate fifth kinds (counterfactual, promissory, negative) and all decomposed into combinations of the existing four. New finding F8: **Claim coupling has two sub-kinds** — moment-pattern (Outcome=Success at τ_s = end) and trajectory-pattern (Argument's resolution across the whole narrative; Judgment=Bad as a gradual descent). Both require verifier primitives but the shapes are different. F1–F7 all generalized from Oedipus; F6 and F7 strengthened by Macbeth's reliance on trajectory-shape verification and derivation-composed checks. Implications: `lowering-record-sketch-01` is ready to draft (confidently scoped to Realization only); `verification-sketch-01` as a sibling should specify both moment-pattern and trajectory-pattern primitives; dialect-level schemas should declare coupling kind per record type. Next actions named: draft the record-shape sketches, author `macbeth.py`, extend `oedipus.py`, and (deferred) run a third exercise on a structurally-dissimilar-again story (comedy / ensemble / lyric).
- [Lowering record — sketch 01](lowering-record-sketch-01.md) — synthesis sketch informed by the two lowering exercises; specifies the Lowering record's exact shape. Commitments L1–L10: Realization coupling only (L1 is the guardrail); record shape with id, `upper_record` and `lower_records` as `CrossDialectRef` pairs, annotation with descriptions-01-style attention, optional `position_range` for author-declared position correspondence (per F4), `status ∈ {active, pending}` admitting unrealized Lowerings as first-class (per F5), staleness via `anchor_τ_a`; many-to-many binding; lower side admits typed-fact or description targets interchangeably; authored through the proposal queue, never synthesized; contains no verification logic (L10 defers verification to its sibling sketch). Sister to verification-sketch-01; the two form the load-bearing synthesis pair.
- [Verification — sketch 01](verification-sketch-01.md) — sibling synthesis sketch covering the coupling kinds Lowering does not: Characterization and Claim (moment-pattern and trajectory-pattern per F8). Commitments V1–V8: automated linting at dialect boundaries (V1); output via the existing proposal queue as observations, never errors (V2); three primitive verifier kinds — Characterization, Claim-moment, Claim-trajectory (V3); verifier speaks upper dialect vocabulary, evaluates via lower dialect queries (V4); coupling kind declared per record-type at the dialect schema level (V5); composes with inference-01's derivation surface for query-shaped checks (V6); partial-match observations first-class with match_strength signal (V7); architecturally distinct from Lowering (V8). Three output record shapes: `VerificationReview`, `StructuralAdvisory`, `VerificationAnswerProposal` — all flow through the existing proposal queue. Explicitly distinguishes cross-boundary verification (this sketch's topic) from *within-dialect* constraint propagation — the "Dramatica-16,384-forms click-into-place" phenomenon belongs to dialect self-verification or a Template-constraint sketch, not here. The two are complementary; they share the proposal queue but handle different concerns.
- [Pressure-shape taxonomy — sketch 01](pressure-shape-taxonomy-sketch-01.md) — parallel to event-kind-taxonomy-sketch-01 at a different granularity: EK2 classifies individual events; LT2 classifies the *arc*. Closes DSP_limit — the last DSP-axis Characterization gap at the dramatica-complete → substrate boundary. Commitments LT1–LT6: verifier classification reads fold-visible structure, not type strings (LT1); Optionlock-shape predicate is the disjunction of three convergence signals — retraction (`asserts=False` unwinding a prior assertion), identity resolution (equivalence-class collapse via `IDENTITY_PREDICATE`), and rule-derivable compound emergence (LT2); Timelock-shape predicate is explicitly weak — complement-only, reports `NOTED` for Timelock-consistent-but-unaffirmed absent substrate-05 scheduling vocabulary (LT3); classifier lives in `verifier_helpers.py` (LT4); author DSP_limit declaration drives verdict polarity, never silently overridden (LT5); the pressure-shape frame is verifier-local, not a substrate commitment (LT6). Sketch-01 extended by [sketch-02](pressure-shape-taxonomy-sketch-02.md) (LT7–LT11; strong Timelock predicate per the Rocky cross-boundary reader-model probe's signature proposal). Sketch-01's LT3-weak fallback remains in place for the Timelock-declared + no-signals case; LT2's Optionlock predicate unchanged.
- [Pressure-shape taxonomy — sketch 02 (LT3-strong)](pressure-shape-taxonomy-sketch-02.md) — amendment (not supersession) committing the probe-proposed strong Timelock predicate. Commitments LT7–LT11 extend sketch-01's LT1–LT6 without retouching them: arc-position banding for LT2 signals — peripheral-pre (τ_s<0 per substrate convention), terminal (top 10% of positive-arc range, gated on > 3 positive events), middle-arc (remainder) — distinguishing arc-converging from arc-peripheral signals (LT7); scheduling-predicate recognition via the verifier-local `scheduled_*` name convention (LT8); strong Timelock predicate requiring both LT8 signal present AND middle-arc LT2 count = 0 (LT9); revised LT5 disposition table with APPROVED for Timelock-strong, PARTIAL for optionlock-peripheral, NOTED for sketch-01 weak-fallback cases (LT10); back-compat guarantee — sketch-01's `classify_arc_limit_shape` 3-tuple signature preserved; new `classify_arc_limit_shape_strong` ships alongside with the richer dict return (LT11). Rocky's DSP_limit shifts NEEDS_WORK 0.33 → **APPROVED 1.00** (two distinct `scheduled_fight(...)` Props — Mac's original + Rocky's rescheduled — driving LT8 count to 2 and strength to min(1.0, 2/2) = 1.0; prediction was 0.50 assuming one distinct Prop). Oedipus / Macbeth / Ackroyd stay APPROVED 0.67 (middle-arc signal kinds unchanged). The probe/verifier loop closes its first concrete cycle: probe dissent on 2026-04-17 proposed the signature; sketch-02 adopts it; Rocky's declaration/substrate disagreement becomes affirmative agreement. Post-sketch-02 four-point spectrum is **all APPROVED** with Rocky at a distinct strength point documenting its Timelock-strong composition.
- [Resolve-endpoint — sketch 01](resolve-endpoint-sketch-01.md) — closes v3's second cross-corpus DSP_resolve finding (MC end-state behavior, not just mid-arc transition). Commitments RE1–RE5: fold-visible reader (RE1); end-state behavioral-shift predicate for Change encodings — compare MC's EK2 external-action ratio pre vs. post transition, shift ≥ 0.3 fires detection (RE2); core-drive-persistence note for Steadfast encodings — verify the defining fact holds at τ_end; report state-additions (like Rocky's `articulated_goal` entering at τ_s=45) separately from paradigm-reversal (RE3); per-encoding enrichment (RE4); shared helper `compute_pre_post_action_ratios` (RE5). **Measurements (verdict polarity unchanged)**: Oedipus RE2 fires — external-action 90% → 33% across anagnorisis (investigation → self-blinding/exile) — Change strengthened at end-state layer; Macbeth RE2 no shift — 75% → 71%, both halves action-dominated, "I will not yield" doubling-down surfaced honestly; Rocky RE3 — `articulated_goal` holds at τ_end, state-addition at τ_s=45 noted distinctly from paradigm-reversal; Ackroyd RE3 — `betrayer_of_trust` pre-arc-anchored with no state-additions, Steadfast at both layers. **Third landing where verdict polarity doesn't change** (after BW4 and RR3). Seventh concrete probe → sketch cycle. Second layer on the DSP_resolve axis (after RR3's IC-relational layer) — the DSP_resolve axis has now produced three successive probe-surfaced layers across three passes.
- [Resolve-relational — sketch 01](resolve-relational-sketch-01.md) — **first cross-corpus probe-driven sketch**: closes the v2 (2026-04-17) cross-boundary reader-model probe's coherent finding that all four encodings' DSP_resolve checks used MC-state proxies when Dramatica's Resolve axis is fundamentally MC↔IC relational. Commitments RR1–RR6: fold-visible reader (RR1); temporal correlation window predicate `detect_preceding_ic_event(target_τ, ic_event_τs, window=5)` returning `has_correlation / nearest_preceding_ic_τ / gap / ic_events_in_window` (RR2); additive IC-relational signal in each DSP_resolve check — Change encodings check IC events preceding MC inflection; Steadfast encodings count IC pressure events the MC held through (RR3); shared helper in `verifier_helpers.py` (RR4); per-encoding IC throughline id declaration — `T_impact_jocasta` / `T_impact_lady_macbeth` / `T_ic_poirot` / `T_ic_apollo` (RR5); verifier-local frame (RR6). Zero substrate changes; complementary `events_advancing_throughline(throughline_id, lowerings, scenes, fabula)` fallback helper resolves IC events via `Scene.advances` for encodings without a direct IC-throughline Lowering (Oedipus, Rocky). **Measurement**: all four DSP_resolve verdicts stay APPROVED 1.0; comments enriched with RR3 IC-signals (Macbeth: LM precedes tyrant-transition by gap=1; Oedipus: Jocasta realize at τ_s=9 precedes anagnorisis τ_s=13 by gap=4; Ackroyd: Sheppard resists 4 Poirot events; Rocky: Rocky resists 9 Apollo events). **Second landing where verdict polarity doesn't change** (after BW4) — honesty about what the substrate encodes at the IC-relation layer is the deliverable. Sixth concrete probe → sketch cycle in two days.
- [Beat-weight taxonomy — sketch 01](beat-weight-taxonomy-sketch-01.md) — closes the Macbeth cross-boundary reader-model probe's DA_mc qualification (2026-04-17) via a canonical **beat-type weight vocabulary** applied to the Activity-domain ratio computation. Commitments BW1–BW5: fold-visible classifier (BW1); five canonical weights — `inciting=2.0, rising=1.0, midpoint=2.0, climax=2.0, denouement=1.5` — per the Dramatic dialect's beat vocabulary (BW2); `event_to_beat_type(event_id, throughline_id, lowerings, scenes, beats)` resolver walking Lowerings → Scene.advances → Beat.beat_type (BW3); Macbeth DA_mc uses weighted ratio (BW4); weighting frame verifier-local (BW5). **Macbeth DA_mc shift**: PARTIAL 0.69 → **PARTIAL 0.65** (raw 9/13 → weighted 11.0/17.0). **First landed probe proposal where verdict polarity does not change** — the probe predicted "the 0.69 strength may even be generous", and the weighted measurement validates that: the non-Activity events (inciting prophecy, midpoint ghost) are dramatically load-bearing, so the honest signal is lower than raw counting suggests. Oedipus / Ackroyd / Rocky DA_mc unchanged (Macbeth-only wiring; each encoding's check composes whichever classifiers earn their keep). Fifth concrete probe → sketch → measurement-shift cycle in two days. Closes the last banked probe proposal from the four-encoding spectrum.
- [Event-manipulation taxonomy — sketch 01](event-manipulation-taxonomy-sketch-01.md) — closes the Ackroyd cross-boundary reader-model probe's DA_mc qualification (2026-04-17) via a **knowledge-asymmetry** predicate: an event is concealment-manipulation-shaped wrt MC iff MC holds a self-fact (world-proposition whose first arg is MC_id) at `Slot.KNOWN` that at least one non-MC participant in the event does not. Commitments MN1–MN5: fold-visible classifier (MN1); concealment-asymmetry predicate (MN2); classifier lives in `verifier_helpers.py` as `classify_event_manipulation_shape(event, mc_id, events_in_scope)` (MN3); Ackroyd DA_mc check composes MN3 with `MANIPULATION_KINDS` via OR — type-set as fast path, MN2 for investigation events where Sheppard is `also_present` / `assistant` (MN4); verifier-local frame (MN5). **Ackroyd DA_mc shift**: PARTIAL 0.54 → **APPROVED 0.85** (11/13 MC-Throughline events manipulation-shaped — 7 via type-set + 6 via concealment-asymmetry). Oedipus / Macbeth / Rocky DA_mc unchanged (MN4 is Ackroyd-only wiring; their checks use EK2). Fourth concrete probe → sketch → measurement-shift cycle in two days.
- [Identification-goal trajectory — sketch 01](identification-goal-sketch-01.md) — closes the Oedipus cross-boundary reader-model probe's Story_goal qualification (2026-04-17) by distinguishing **identification-shaped** Story_goals (arrival = investigator's knowledge supports a target derivation) from **achievement-shaped** goals (arrival = a world-state fact holds). Commitments IG1–IG5: goal-kind distinction (IG1); identification-goal trajectory reads `project_knowledge` + `holds_derived` not `project_world` + `world_holds_derived` (IG2); premise order for identification goals is knowledge-accrual order, not fabula-order — the prior "unusual premise order" penalty on Oedipus was a category error (IG3); achievement goals unchanged (IG4); verifier-local distinction (IG5). **Oedipus Story_goal shift**: PARTIAL 0.7 → **APPROVED 1.0** (the check now finds `parricide(oedipus, laius)` first supportable from Oedipus's KnowledgeState at τ_s=13 via identity-substitution through the anagnorisis). Ackroyd's Story_goal check — which was already reading `project_knowledge` for witness-set expansion — becomes the formal precedent; no code change to Ackroyd. Macbeth's and Rocky's achievement-goal checks untouched. Third concrete probe → sketch → measurement-shift cycle in two days (after LT9 and AG5).
- [Event-agency taxonomy — sketch 01](event-agency-taxonomy-sketch-01.md) — parallel to event-kind-taxonomy-sketch-01 and pressure-shape-taxonomy-sketch-01/02 at the DSP_growth axis. Closes the Oedipus cross-boundary reader-model probe's DSP_growth dissent (2026-04-17) by distinguishing **agentive-pursuit** events (MC initiates investigation / confrontation — investigation-era listener events where MC summoned the speaker fire here) from **reactive-consequential** events (MC's state is targeted by a world-effect; self-directed harm, exile, consequence-landing). Commitments AG1–AG6: classifier reads fold-visible structure (AG1); consequential predicate = any WorldEffect whose prop's first arg is mc_id (AG2); pursuit predicate = MC participates + not consequential + has non-null effects (AG3, permissive by complement); classifier lives in `verifier_helpers.py` as `classify_event_agency_shape(event, mc_id)` (AG4); DSP_growth=Stop check measures pursuit-event-count drop across arc pivot, not raw participation rate (AG5); agency frame verifier-local (AG6). **Oedipus DSP_growth shift**: PARTIAL 0.5 → **APPROVED 1.0** (pursuit events drop 5→0 across the anagnorisis pivot; post-anagnorisis events are all consequential-shaped — self-blinding, exile). Other encodings' DSP_growth checks unchanged — Macbeth's kill-accumulation is already pursuit-oriented (counting the behavior that needs to stop); Ackroyd and Rocky's DSP_growth=Start are on a different axis. Second concrete probe → sketch → measurement-shift cycle in two days (after pressure-shape-taxonomy-sketch-02's Rocky DSP_limit shift).
- [Event-kind taxonomy — sketch 01](event-kind-taxonomy-sketch-01.md) — narrow sketch closing the verifier-layer weakening exposed by the three-point verdict-strength spectrum (APPROVED 0.69 Macbeth / PARTIAL 0.54 Ackroyd / NEEDS_WORK 0.31 Oedipus post-F5). The `ACTIVITY_ACTION_KINDS` / `INTERNAL_STATE_KINDS` frozen type-string sets in the three dramatica-complete → substrate verifier modules lump every `utterance`-typed event into the internal-state bucket — correct for soliloquy, wrong for command, interrogation, confrontation, and testimony, which are Sophocles's onstage grammar. Commitments EK1–EK5: verifier classification reads fold-visible structure, not type strings (EK1); external-action predicate is a structural function of participants and effects — interpersonal AND outward-effect (EK2); classifier lives in `verifier_helpers.py` (EK3); substrate-05's per-story type-split extension rule remains the fallback when participants/effects can't distinguish cases (EK4); the external-vs-internal frame is verifier-local, not a substrate commitment (EK5). Zero substrate changes; removes the brittle type-string heuristic from all three existing verifiers. After the implementation pass, the three-point spectrum measures substrate-lowering honesty rather than verifier-heuristic brittleness — the signal it was meant to carry. Closes planned item 1 (event-kind taxonomy sketch) below.
- [Save the Cat dialect — sketch 01](save-the-cat-sketch-01.md) — second upper-dialect under architecture-sketch-02's stack, after Dramatic. Encodes Blake Snyder's 15-beat framework as a prescriptive (where Dramatic is parameterized), beat-driven (where Dramatic is role-driven), page-positioned (where Dramatic uses abstract narrative_position) sibling. Commitments S1–S8: 15 canonical beat slots fixed in name and order (S1); page targets as positional coordinate (S2); A and B Story strands as first-class (S3); theme statement at Story level (S4); Genre as a Template-shaped record with ten ships-as-data genres (S5); self-verifier within dialect vocabulary, observation-only never error (S6); description surface inherited from the future Dramatic-side state (S7); connective relations to other dialects out of scope here (S8). Worked example sketched: Macbeth-as-Save-the-Cat, with the imperfect B Story fit as a feature of the example. The dialect's value proposition for the on-ramp is the *familiar surface* — most working commercial writers know Save the Cat; few know Dramatica.
- [Save the Cat dialect — sketch 02 (StcCharacter amendment)](save-the-cat-sketch-02.md) — amendment (not supersession) closing the StcCharacter gap flagged after Macbeth and pressed harder by Ackroyd. Commitments S9–S13 extend sketch-01's S1–S8 without retouching them: StcCharacter as a minimal optional record with id/name/description/role_labels (S9); a canonical role-label vocabulary of ten labels that the verifier recognizes, with open strings admissible for per-genre archetypes and the long tail (S10); optional reference wiring — `StcBeat.participant_ids`, `StcStrand.focal_character_id`, `StcStory.character_ids` (S11); `StcArchetypeAssignment` binding each genre archetype to either a character_id or a prose note (S12); three new verifier checks — character-id resolution, one-protagonist advisory, archetype coverage (S13). The load-bearing structural claim the amendment makes expressible is the Ackroyd case: Sheppard carries `role_labels=("protagonist", "antagonist", "narrator")` on a single character — the MC-Antagonist-Narrator overlap Christie's novel pivots on, which under sketch-01 was "a person referenced in beat descriptions." Macbeth under sketch-02 demonstrates the prose-note side: Rites of Passage's three archetypes ("the life problem", "the wrong way", "the acceptance") bind via notes rather than characters — the dialect's honest signal that genre archetypes don't always map to people. Both encodings continue to verify with exactly 1 NOTED observation (same as pre-amendment).

**Cross-cutting (UX direction, not yet committed to):**

- [Author document surface — sketch 01](author-document-surface-sketch-01.md) — captures the direction of an author-facing document format that parses INTO the Python record kernel rather than replacing it. Motivation: a `.py` file says *this is code, leave it to the engineer*; a `.md` or `.story` file says *this is yours, go ahead.* Two candidate shapes considered: Markdown with fenced declarative blocks (recommended starting point — familiar, diff-friendly, AI-and-human-symmetric since AI consumes the same source text the human edits, forward-compatible with future renderers), and a story-flavored mini-DSL (cleaner read but a custom grammar to maintain). The Python records remain the canonical surface and source of truth in either case; the document layer is one-directional. Status: captured-not-committed; the project stays in Python for research / design / experiment mode and revisits when a non-engineer author bounces off the Python files or the count of high-level dialects past two or three makes the per-dialect Python authoring overhead the gating concern.

## Upcoming sketches

Planned work. Each is a sketch-to-write or concrete prototype task; filenames are provisional. Dependency order is approximate; sketches advance when a forcing function argues for it (e.g., the descriptions surface shipped write-only, which advanced reader-model; Jocasta's anagnorisis had a clean failure mode, which advanced identity-and-realization; the `project_reader` weakening had a clean forcing function in both encodings, which advanced focalization; the Oedipus parricide/incest encoding exercised the compound-predicate pinch, which advanced the inference model).

**Recently landed (not sketched, implemented):** the Lowering record + three verifier primitives (Characterization, Claim-moment, Claim-trajectory) ship in `lowering.py` and `verification.py` with walker integration via `proposal_walker.py`'s `walk_verifier_results` and `walk_annotation_reviews`. Per-record-type coupling declarations live on `dramatic.py` (17 declarations), `save_the_cat.py`, and now `dramatica_template.py` (17 Template declarations + DSP per-axis map). The inference model (`Rule`, `holds_derived`, `derive_all`, proof-carrying derivation) is in `substrate.py`, with the author-asserted compound predicates in `oedipus.py` (parricide/incest) and `macbeth.py` (kinslayer/regicide/breach_of_hospitality/tyrant) now retired in favor of rule derivation. Six dramatica-complete encodings exist (Oedipus, Macbeth, Ackroyd, Pride and Prejudice, Rocky, Chinatown) — the canonical Outcome × Judgment matrix is closed across all four corners (personal-tragedy ×3, triumph, personal-triumph, tragedy); every DSP axis direction is exercised at least once across the corpus. The Dramatica character-element decomposition ships complete (Motivation + Methodology + Evaluation + Purpose = 64 elements across 4 dimensions, 16 Issue Quads). **Four verifiers** at the **dramatica-complete → substrate** boundary shipped — Macbeth / Oedipus / Ackroyd, plus Rocky (first Timelock + Failure + Judgment=Good for MC in the corpus, authored as the three-phase Rocky encoding alongside pressure-shape-taxonomy-sketch-01). Nine-check shape across all three primitives; content adapted per encoding. Findings: (1) **no Realization couplings at the Template layer** — every Template record is Characterization or Claim, so all new coupling work lives in the verifier surface rather than in a `*_lowerings.py` sibling; (2) **verdict strength as substrate-completeness meter, for free** — a real three-point spectrum visible across the Characterization primitive at DA_mc. Post-EK2: APPROVED 0.77 (Oedipus), PARTIAL 0.69 (Macbeth), PARTIAL 0.54 (Ackroyd — Manipulation check, EK2 does not cover), APPROVED 0.72 (Rocky). Pre-EK2 the Oedipus verdict sat at NEEDS_WORK 0.31, which turned out to be verifier-heuristic brittleness rather than substrate-encoding honesty; EK2 removed the type-string-dependence and the real signal emerged. (3) **F5 extension landed and EK2 followed** — F5 authored Tiresias, Creon, and the post-anagnorisis beats in `oedipus.py`; 6 PENDING Lowerings flipped ACTIVE. F5 moved DA_mc / DSP_approach 0.20 → 0.31. The residual NEEDS_WORK then drove event-kind-taxonomy-sketch-01, whose EK2 structural predicate pushed both verdicts from 0.31 to 0.77 without any further substrate changes. The two-step F5-then-EK2 trajectory is the model for "substrate extension alone is necessary but not always sufficient — verifier-heuristic quality matters too." (4) **Claim-moment and Claim-trajectory primitives are robust across substrate shapes** — and Rocky's Phase 2 landing generalized two of them with new verifier patterns: Steadfast-via-structural-invariance (versus Ackroyd's pre-existing-trait shape) and Failure-via-goal-opposite-evidence (versus the three Success encodings' direct-fact-check). Same primitive, different evidence shape per encoding. (5) **DSP_limit four-point spectrum: sketch-01 landed real disagreement (Rocky NEEDS_WORK 0.33); sketch-02 closed it into affirmative agreement** (Rocky APPROVED 1.00). Sketch-01's LT5 asymmetric disposition validated Rocky's disagreement concretely; sketch-02's LT7–LT11 amendment (scheduling-predicate recognition + arc-position banding) adopted the cross-boundary reader-model probe's proposed signature and lifted the verdict without retouching the substrate.

1. *(Retired — landed as EK1–EK5 in [event-kind-taxonomy-sketch-01](event-kind-taxonomy-sketch-01.md).)* `classify_event_action_shape` and `agent_ids_from_entities` ship in `verifier_helpers.py`; the two Characterization checks in all three dramatica-complete verifier modules call EK2; the old type-string frozen sets are removed from the Oedipus and Macbeth verifiers (Ackroyd keeps `MANIPULATION_KINDS` because EK2 does not classify Manipulation-shape). **Post-EK2 measured spectrum:** Oedipus DA_mc / DSP_approach: NEEDS_WORK 0.31 → **APPROVED 0.77 / APPROVED 0.77** (+46 pp each; 2-of-4 → 4-of-4 APPROVED); Macbeth DA_mc holds at PARTIAL 0.69, DSP_approach APPROVED 0.79 → APPROVED 0.71 (EK2 stricter than the type-string heuristic, one event drops out as expected); Ackroyd DSP_approach PARTIAL 0.38 → PARTIAL 0.46 (Be-er inversion preserved via complement); all Claim-moment / Claim-trajectory results unchanged. 15 new tests pin the classifier and the post-EK2 verdict bands. **Finding:** the pre-EK2 Oedipus NEEDS_WORK verdict was verifier-heuristic brittleness, not substrate-encoding honesty; the surviving three-point spread (APPROVED 0.77 / PARTIAL 0.69 / PARTIAL 0.54) is now truthful signal about encoding/taxonomy match — the spectrum's intended meaning.
2. **Extend the dramatica-complete → substrate verifier surface** (concrete prototype work — *partial landing 2026-04-16, extended same day, Rocky Phase 2 same day*) — **four** verifiers (Macbeth / Oedipus / Ackroyd / Rocky), each shipping nine checks: the original four (DA_mc, DSP_approach, DSP_outcome, DSP_judgment) plus DSP_resolve, DSP_growth, Story_goal, Story_consequence, and DSP_limit. Exercises all three primitives more fully — claim-trajectory at Story_goal and two more DSP axes; claim-moment at Story_consequence; characterization at DSP_limit. Findings: **(a) DSP_resolve and DSP_judgment are Dramatica-distinct even when they land on the same substrate fact** (the tyrant / self-identity emergence is both a transition-point and a final-state-quality); **(b) DSP_growth inversion works across Start/Stop** (Oedipus/Macbeth check for failure-to-stop; Ackroyd for ultimatum-compelled start; Rocky for privately-chosen start at the articulated-goal moment — same verifier predicate, three different authorial polarities); **(c) Story_consequence PARTIAL 0.5 on Ackroyd was a substrate gap, not a verifier bug — fix landed same day.** The Ralph-cleared retraction paid twice (Story_consequence rescue + Ackroyd's Optionlock signal at DSP_limit). The sibling STC verifier's Strand_B trajectory check was updated in the same pass; **(d) Story_goal PARTIAL 0.7 on Oedipus** surfaces a world-state-vs-knowledge-trajectory distinction worth design attention — goal-as-identification is an agent-knowledge claim, not a world-state claim; **(e) DSP_limit four-point spectrum lands meaningful disagreement**: three Optionlock encodings APPROVED 0.67 (Oedipus via identity-resolution + rule-emergence; Macbeth and Ackroyd via retraction + rule-emergence, three distinct signal compositions); **Rocky's Timelock declaration produced NEEDS_WORK 0.33 under sketch-01 — the first non-APPROVED DSP_limit in the corpus — then shifted to APPROVED 1.00 under sketch-02's LT7–LT11 (2026-04-17)**. Rocky's substrate shows retraction:1 (Mac's scheduled fight, pre-plot τ_s=-5) + rule-emergence:1 (went_the_distance, terminal τ_s=55), both non-middle-arc under LT7 banding; `scheduled_fight` predicates (Mac + Rocky versions) fire LT8; LT9-strong classification results. LT5's asymmetric disposition validated concretely under sketch-01; LT3's OQ3 ("arc-peripheral vs. arc-converging signals") retired as the forcing function for sketch-02; **(f) Rocky's Phase 2 landing generalized two verifier patterns**: Steadfast-via-structural-invariance (zero equivalence-class collapses involving the MC — distinct from Ackroyd's pre-existing-trait Steadfast) and Failure-via-goal-opposite-evidence (the compound-derivation that contradicts the goal IS the evidence supporting Failure; inverted from the three prior Success encodings' direct-fact checks); **(g) Judgment=Good is a positive-closure confluence, not a trajectory shape** (Rocky's Good rests on three facts holding simultaneously at τ_s=end — achievement + relationship + acceptance — rather than an emerges-and-persists pattern like the Bad-judgment cases). Remaining at this boundary (still open): 16 Signpost claim-moment checks per encoding; up-to-32 CharacterElementAssignment claim-trajectory checks per encoding across Motivation/Methodology/Evaluation/Purpose; ThematicPicks pick-chain checks; remaining 3 DomainAssignments per encoding (OS, IC, RS); LT3-strong Timelock predicate *landed* as sketch-02's LT7–LT11 (2026-04-17) — zero substrate changes required; verifier-local `scheduled_*` predicate convention + LT7 arc-position banding closed the OQ. P&P and Chinatown remain gated by substrate authoring (no substrates yet); Rocky substrate landed 2026-04-16.
3. *(Retired — landed as save-the-cat-sketch-02.)* StcCharacter, canonical role labels, reference wiring, archetype assignments, and three new verifier checks ship in `save_the_cat.py`; `macbeth_save_the_cat.py` and `ackroyd_save_the_cat.py` now author character records and archetype assignments. Sheppard carries `role_labels=("protagonist", "antagonist", "narrator")` — the structural claim the Ackroyd comparison sketch named as unexpressible under sketch-01. Both encodings continue to verify with exactly 1 NOTED observation (unchanged from pre-amendment). 27 new tests pin each S13 observation code plus integration contracts on both encodings.
4. **Cross-boundary reader-model probe** (concrete prototype work — *two four-encoding Template-surface spectrum runs landed 2026-04-17; prior partial 2026-04-16 at the Dramatic surface on Macbeth*) — `dramatic_reader_model_client.py` extended to render Template records (DomainAssignments, DynamicStoryPoints, Signposts, ThematicPicks, CharacterElementAssignments, Story_goal / Story_consequence) alongside Dramatic records when present; Dramatic-only callers unaffected. Four Template-surface probe runs (Rocky, Macbeth, Oedipus, Ackroyd), each saved as JSON for reference. Full spectrum: **96 annotation reviews** (90 approved + 6 needs-work) and **36 verifier commentaries** (27 endorses + 7 qualifies + 2 dissents), 0 dropped across all four runs. Findings: **(a) DSP_limit dissent landed as sketch-02 same day** (Rocky): probe argued Rocky's Timelock IS well-supported and proposed a concrete scheduling-endpoint signature; sketch-02 adopted it as LT7–LT11; Rocky's DSP_limit shifted NEEDS_WORK 0.33 → APPROVED 1.00 — first concrete cycle of the probe/verifier loop; **(b) probe behavior is remarkably consistent across encodings** — ~7 endorses + 2 qualifies baseline per 9-check run; dissent is rare (2 across 4 encodings, both on genuine predicate weaknesses). The probe acts as a steady signature-proposal engine, not a verdict-opposition machine; **(c) Oedipus is the annotation-quality outlier**: 3 needs-work annotations (L_tiresias mislabels the IC Throughline; L_jocasta_doubt_speech omits the relationship-throughline beat the Scene advances; L_discovery_and_crown_pending references a dangling non-existent Dramatic Scene) versus 1 each on Rocky / Macbeth / Ackroyd — the probe catches real encoding defects below verifier granularity. Worth an Oedipus-hygiene pass; **(d) four new signature-upgrade proposals** parallel to Rocky's LT3-strong: **Oedipus DSP_growth dissent — landed 2026-04-17 as [event-agency-taxonomy-sketch-01](event-agency-taxonomy-sketch-01.md) (AG1–AG6)**; probe's agentive-pursuit vs. reactive/consequential distinction adopted, Oedipus DSP_growth PARTIAL 0.5 → APPROVED 1.0 (pursuit events drop 5→0 across the anagnorisis pivot). Second concrete probe → sketch → measurement-shift cycle in two days; **Oedipus Story_goal qualification — landed 2026-04-17 as [identification-goal-sketch-01](identification-goal-sketch-01.md) (IG1–IG5)**; identification-vs-achievement goal distinction adopted, Oedipus Story_goal PARTIAL 0.7 → APPROVED 1.0 (knowledge-projection replaces world-projection; recognition τ_s at the anagnorisis IS the trajectory landing). Third concrete probe → sketch → measurement-shift cycle in two days; **Ackroyd DA_mc qualification — landed 2026-04-17 as [event-manipulation-taxonomy-sketch-01](event-manipulation-taxonomy-sketch-01.md) (MN1–MN5)**; knowledge-asymmetry predicate adopted (MC knows self-fact at KNOWN; another participant doesn't), composed with existing `MANIPULATION_KINDS`. Ackroyd DA_mc PARTIAL 0.54 → APPROVED 0.85. Fourth concrete probe → sketch → measurement-shift cycle in two days; **Macbeth DA_mc qualification — landed 2026-04-17 as [beat-weight-taxonomy-sketch-01](beat-weight-taxonomy-sketch-01.md) (BW1–BW5)**; canonical beat-type weighting adopted; Macbeth DA_mc PARTIAL 0.69 → PARTIAL 0.65 (first landing that refines-rather-than-flips a verdict — honesty-about-measurement is the deliverable). Fifth concrete probe → sketch → measurement-shift cycle in two days. Closes all four banked probe proposals. Each is a candidate sketch-02-style amendment when a forcing function argues for it; **(e) probe/verifier coordination model stabilizes** (verification-sketch-01 OQ4 answered concretely across 4 runs): ~75% endorse + ~19% qualify + ~6% dissent. The two partner cleanly — verifier provides mechanical verdicts; probe provides methodology/substance review. **(h) Fourth probe pass (v4) after RE lands** (saved as `*_dramatica_complete_output_v4.json`): total commentary distribution 29E/7Q/0D (identical to v3). **The loop has reached a stable plateau at the commentary level after 4 passes.** However the *content* of qualifications has shifted character: v2/v3 surfaced architectural patterns (cross-corpus findings requiring new sketches); v4 surfaces implementation-refinement observations within landed sketches — Rocky DSP_resolve flags that RR3's Scene.advances fallback picks up "temporal-scope" events without IC-participation (4 of 9 cited); Macbeth DSP_resolve suggests v3's "I will not yield" bracket should be upgraded from a note to a verdict-driving signal; **Ackroyd DSP_approach surfaces a new cross-sketch composition pattern** — use MN2 concealment-asymmetry as a secondary Be-er indicator for DSP_approach (signals from one sketch should be available across axes). Oedipus DSP_approach / DA_mc independence concern persists three passes running. Annotation issues persist (Oedipus's L_discovery_and_crown_pending still REJECTED four passes in a row). **Interpretation**: the probe/verifier loop's *architectural* depth converged at ~3 passes; the *refinement* depth is open-ended. Cross-sketch composition is the new forcing function. **(g) Third probe pass (v3) after RR3 lands**: total commentaries 27E/7Q/2D (v1) → 28E/8Q/0D (v2) → **29E/7Q/0D (v3)** — endorses monotonically increase; dissents stay at 0 post-v2; qualifies hold steady. The partnership is NOT converging to universal endorsement; instead, each pass **shifts the probe's attention to the next-sharper layer on already-examined axes**. Specifically, v3 produces a **second cross-corpus pattern** (sharper than v2's): three of four DSP_resolve checks (Rocky Steadfast, Macbeth Change, Oedipus Change) qualify around MC *end-state* behavior rather than mid-arc transition alone — Rocky's `articulated_goal` entering at τ_s=45 IS a state change though not a paradigm change; Macbeth's "I will not yield" terminal beat could read as Steadfast commitment despite the mid-arc tyrant-transition; Oedipus's self-blinding could be read as embodied IC-paradigm adoption (the OQ1 banked in IG2 crystallizes into a concrete end-state-alignment proposal). Ackroyd's DSP_resolve flips to **endorse** in v3 — RR3's IC-resistance signal closed its v2 qualification. Secondary findings: DSP_approach independence (repeated from v2; probe suggests agent-vs-listener role distinction or MN2 composition as secondary Be-er signal); Macbeth DA_mc event-type-to-domain mapping question (prophecy_received as Learning sub-type of Activity). Annotation-quality findings persist (Oedipus's L_discovery_and_crown_pending still REJECTED, L_tiresias still needs-work; Macbeth L_banquo_killing still needs-work; Ackroyd L_blunt still needs-work). **Interpretation**: the loop depth appears open-ended — each pass produces sharper signatures rather than converging to endorsement-everywhere; the DSP_resolve axis has now produced three successive findings (v1 qualification → v2 RR3 relational → v3 end-state). **(f) Second probe pass (v2) after all five sketches landed** (saved as `*_dramatica_complete_output_v2.json`): the loop **converged on previously-dissented dimensions** — 0 dissents across 4 encodings (was 2 in v1); previously-dissented DSP_limit (Rocky), DSP_growth (Oedipus), and previously-qualified DA_mc (Ackroyd), Story_goal (Oedipus), DA_mc (Macbeth) all endorse or still qualify without the v1-signatures remaining open. The ~7E/2Q/0D baseline is preserved per encoding. **New coherent finding v2**: all four encodings' DSP_resolve checks drew qualifications along the same axis — the current substrate-proxy checks (identity-transition / world-state-stability / etc.) don't measure *Dramatica's specific IC-relationship shape*; Resolve axis is fundamentally about the MC's response to IC influence, not about MC-state-alone. Candidate DSP_resolve-strengthening sketch — relational-signal detection between MC and IC throughline events — is banked as the next forcing function. Also new: DSP_approach / DA_mc signal-independence concern across Oedipus and Rocky (both checks use EK2 against the same event sets); annotation-quality findings persist (Oedipus's stale L_discovery_and_crown_pending dangling Scene now flagged as **rejected** rather than needs-work — escalated critically; one new Macbeth L_banquo_killing kinship wording issue; one new Rocky L_training_montage missing-IC-beat-coverage note). Remaining: edit proposals on Lowerings (annotation reviews land; edit proposals to rewrite Lowering annotation prose would close item 4 fully); P&P and Chinatown Template probes gated on substrate authoring.
5. **Dramatic dialect — sketch 02** (provisional) — addresses dramatica-template-sketch-01's six forcing functions. **Also the likely home for Template-level constraint-propagation** — Dramatica's "16,384 valid story forms" with the "click-into-place" authoring experience (per discussion around verification-sketch-01) is a within-dialect concern, not cross-boundary verification. Priority depends on whether dramatica-complete needs richer storyform constraint checking soon.
6. **Structural dialect — sketch 01** (provisional, lowered-priority) — parallel upper dialect to Dramatic. Both lowering exercises confirmed direct Dramatica→substrate coupling works; a middle Structural dialect remains useful but not required. Stays provisional until a concrete case shows it earning its keep.
7. **Inference model — sketch 02** (provisional) — follow-ons to inference-sketch-01 as forcing functions emerge. Likely candidates: typed inequality / NAF for `sibling`-family rules; rule-anchored descriptions; rule attention; refining N9 under culturally-loaded compound predicates.
8. **View vs. sjuzhet visibility** (provisional, banked from the Oedipus probe walk) — the `ReaderView` exposes substrate state (τ_s, τ_a) but not the sjuzhet schedule (τ_d). Descriptions that reference disclosure-schedule coordinates ("the reader has held both since τ_d=0") make claims the view cannot verify. See `D_view_cannot_see_τ_d` in the Oedipus encoding.
9. **256 Element Quads (canonical Dramatica theory data)** (deferred, large) — the Dramatica template currently ships the Domain Quad, four Concern Quads, 16 Issue Quads, and four sets of 16 character-element quads. The deepest level — Element Quads under each Issue (the Problem level) — is 64 × 4 = 256 quads with ~1,024 labels. Mechanical but large; deferred until an encoding actually pressures the Problem level (none of the six has yet — all stop at Issue). OQ5 from dramatica-template-sketch-01 asks where this data lives (inline, external, author-supplied) — decision pending.

The reader-model probe now exercises the view against live Claude Opus 4.6 (`reader_model_client.py`, `demo_reader_model.py` for Rashomon, `demo_reader_model_oedipus.py` for Oedipus), ingests structured reviews, answer proposals, and edit proposals as substrate-native records, and surfaces a proposal queue the author can walk interactively (`proposal_walker.py`, driven by `--walk`). The walker routes accept decisions through `ingest_review`, `accept_answer_proposal`, and `accept_edit_proposal`; declines through `decline_proposal`. Edit acceptance supersedes the source description (marks it `DescStatus.SUPERSEDED`, hides from default views) and commits a new record at a fresh τ_a, with `supersedes` / `superseded_by` metadata pointers preserving the chain — closing the bounded case of descriptions-sketch-01 OQ1 (description versioning) and reader-model-sketch-01 OQ6 (description-edit proposals). Persistence of the queue across sessions is still deferred per descriptions-sketch-01 OQ3.

The cross-boundary reader-model probe (`dramatic_reader_model_client.py`) runs at the Dramatic surface against Macbeth (`demo_dramatic_reader_model_macbeth.py`) and — following the 2026-04-17 Template extension — at the dramatica-complete surface against Rocky (`demo_dramatic_reader_model_rocky.py`). Output records (`AnnotationReview`, `VerifierCommentary`) share the proposal-queue contract and the same walker integration (`walk_annotation_reviews`, `walk_verifier_commentaries`). Saved live-probe outputs kept as reference data: `reader_model_macbeth_output.json` (Dramatic surface, Macbeth) and `reader_model_rocky_dramatic_output.json` (Template surface, Rocky).

Inference-model-sketch-01 was seeded by a probe walk against the Oedipus encoding: the probe's answer to `D_parricide_incest_authored_not_derived` supplied the derivable-boundary criterion (N9). The sketch's prototype implementation is complete — `Rule`, `holds_derived`, `derive_all`, and proof-carrying derivation live in `substrate.py`; `oedipus.py`'s `parricide` / `incest` and `macbeth.py`'s `kinslayer` / `regicide` / `breach_of_hospitality` / `tyrant` are now derived from rules rather than authored at event sites. A re-probe to see whether the probe reaches for richer rule families (sibling, uncle) that would pressure OQ1 or OQ3 remains open.

## Superseded sketches

Kept as historical record of the design's evolution. Do not use as a starting point.

- [Substrate — sketch 01](substrate-sketch-01.md) — initial substrate statement. Too strong on T1 (single ground truth), conflated slot identity with update source for reader vs. character, too narrow on library claim.
- [Substrate — sketch 02](substrate-sketch-02.md) — delta on 01. Revised T1, K2, and library claim, but left fact/state semantics inconsistent across canonical and contested regions, and retained character-centric slot names.
- [Substrate — sketch 03](substrate-sketch-03.md) — first consolidation. Fixed sketch 02's inconsistencies but conflated three branch uses (contested / draft / counterfactual) under one concept, claimed post-contested canonical consistency "by construction" without a mechanism to back it, and used an undeclared epistemic slot in the worked example.
- [Substrate — sketch 04](substrate-sketch-04.md) — second consolidation. Separated branch kinds (contested / draft / counterfactual), parameterized queries by branch, defined fold scope across branches, and simplified event status to two values. Committed to F1 (emotion/tension as parallel projections), which architecture-sketch-01's A3 later ruled out; left event type's role ambiguous; offered no principled routing rule for adverbial/modal content. Superseded by sketch 05.
